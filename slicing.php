<?php 

// This is a variable or storing string
$x = "Hello World!";

// Using substr to extract a part of the string
// The first parameter is the string, the second is the starting position, and the third is the length of the substring
echo substr($x, 6, 5);
?>